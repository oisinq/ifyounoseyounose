package org.ifyounoseyounose;

import javafx.application.Application;

/**
 * Main - launches GuiManager (the application itself)
 */
public class Main {
    public static void main(String[] args) {
        Application.launch(GuiManager.class, args);
    }
}